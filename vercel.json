{
  "version": 2,
  "builds": [
    {
      "src": "./src/lib/server.js",
      "use": "@vercel/node",
      "config": {
        "command": "npm run build && npm start"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
